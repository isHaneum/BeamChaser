// This file remains as a build placeholder because BeamChaser.xcodeproj
// still references it directly. The actual RunDetailMetricBuilderTests live in
// SessionSyncTests.swift so they stay inside the existing test target without
// requiring project regeneration.